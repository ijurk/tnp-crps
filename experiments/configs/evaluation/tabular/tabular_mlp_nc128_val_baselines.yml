output_dir: results/tabular/mlp_nc128_val_baselines_v1

evaluation_kind: tabular
metric_alpha: 1.0
sampling_seed: 20260724

base_generator_config: experiments/configs/generators/tabular_mlp_nc128.yml

# Match the number of validation samples used during training.
num_eval_samples: 8

# Match the deterministic validation-set budget.
samples_per_eval_set: 4096
eval_batch_size: 16
max_batches: null
device: cuda

models:
  - name: context_mean
    kind: context_mean
    training_alpha: null

  - name: context_gaussian
    kind: context_gaussian
    training_alpha: null

  - name: context_resample
    kind: context_resample
    training_alpha: null

eval_sets:
  - name: mlp_nc128_val
    sampling_seed: 20260724
    overrides:
      # The generator's validation split uses this deterministic seed.
      - generators.test.deterministic_seed=20260722